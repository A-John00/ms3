#include <QCoreApplication>
#include "matrix.h"
#include "cnn.h"
#include <fstream>
#include <iostream>

using namespace std;

int main(int argc, char *argv[])
{
    /*
    matrix* m = new matrix(20, 20, false);
    for(int i = 0; i < 20; i++){
        for(int j = 0; j < 20; j++){
            m->setValue(i,j,i);
        }
    }

    matrix* n = Cnn::MaxPool3D20to10(m);
    n->printM();
    */

    vector<vector<double>> test;

}
